name: test

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ github.base_ref }}
          echo ${{ github.head_ref }}
          echo ${{ github.ref }}
          echo ${{ github.ref_name }}
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
          sleep 2
          echo $(gh api /repos/${{ github.repository }}/commits/${{ github.head_ref }}/check-runs)
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
